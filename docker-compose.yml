services:
  mongodb:
    image: mongo:7
    container_name: veritas-mongo
    ports:
      - "27017:27017"
    volumes:
      - veritas_mongo_data:/data/db
    restart: unless-stopped

volumes:
  veritas_mongo_data:
